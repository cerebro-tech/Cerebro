#!/usr/bin/env bash
# Run paru in RAM

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" &>/dev/null && pwd)"
BUILD_DIR="/tmp/ram_build"
LOG_FILE="$BUILD_DIR/ram_build-paru.log"

mkdir -p "$BUILD_DIR"
echo "[*] Running paru in RAM build dir $BUILD_DIR..."
paru "$@" | tee -a "$LOG_FILE"
