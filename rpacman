#!/usr/bin/env bash
# Run pacman in RAM

BUILD_DIR="/tmp/ram_build"
LOG_FILE="$BUILD_DIR/ram_build-paru.log"

mkdir -p "$BUILD_DIR"
echo "[*] Running pacman in RAM build dir $BUILD_DIR..."
sudo pacman "$@" | tee -a "$LOG_FILE"
